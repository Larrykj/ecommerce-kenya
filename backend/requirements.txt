# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0
email-validator==2.1.1

# Database Drivers (optional - app will work without them)
motor>=3.3.0
pymongo>=4.6.0
redis>=5.0.0

# Machine Learning & Data Science (using pre-built wheels)
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
# scipy>=1.11.0  # Optional - only if needed
# lightfm>=1.17  # Optional - requires C++ compiler

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Configuration & Environment
python-dotenv==1.0.0
pydantic-settings==2.1.0

# HTTP Clients
httpx==0.26.0
requests==2.31.0

# M-Pesa Integration (using direct API calls via requests)
# python-mpesa package not needed - we use Safaricom API directly

# Utilities
python-dateutil==2.8.2
pytz==2024.1
joblib==1.3.2
python-multipart==0.0.6

# Monitoring & Metrics
prometheus-fastapi-instrumentator==6.1.0

# Testing (not needed for deployment)
# pytest==7.4.3
# pytest-asyncio==0.23.2
# pytest-cov==4.1.0

# Image Processing (optional)
# Pillow==10.2.0  # Disabled - optional dependency

# Caching
aiocache==0.12.2

# Background Tasks (optional)
# celery==5.3.4

